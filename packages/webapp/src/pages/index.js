import React from 'react'
import { Router } from '@reach/router'

const IndexRoutes = () => {
  return (
    <Router basepath="/">
      {
        //<SignIn path="/signIn" />
      }
    </Router>
  )
}

export default IndexRoutes
